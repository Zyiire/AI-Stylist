[build]
dockerfilePath = "backend/Dockerfile"
buildCommand = ""

[deploy]
startCommand = "./start.sh"
healthcheckPath = "/search/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
